{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve --dir src",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.6.1",
    "eslint-config-synacor": "^2",
    "if-env": "^1.0.0",
    "semistandard": "^11.0.0"
  },
  "dependencies": {
    "babel-plugin-transform-runtime": "^6.23.0",
    "json-loader": "^0.5.7",
    "preact": "^8.2.6",
    "preact-cli": "^2.1.0",
    "preact-compat": "^3.17.0",
    "preact-redux": "^2.0.3",
    "preact-router": "^2.5.7",
    "redux": "^3.7.2",
    "robust-segment-intersect": "^1.0.1"
  }
}
