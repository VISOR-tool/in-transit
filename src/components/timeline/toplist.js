import { h, Component } from 'preact';

export default class Toplist extends Component {
  // constructor () {
  //   super();
  // }

  render () {

    const attrsText = {
      stroke: '#9E49DE',
      'stroke-width': 0.3,
      fill: 'white'
    };
    const attrs = {
      stroke: 'red',
      'stroke-width': 1,
      fill: 'white'
    };

    return (
      <div> 
        <h2>Toplist</h2>
      </div>
    );
  }
}
